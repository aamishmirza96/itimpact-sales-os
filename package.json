{
  "name": "sales-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite --port 3001 --host\" \"node server/index.js\"",
    "dev:ui": "vite --port 3001 --host",
    "dev:api": "node server/index.js",
    "build": "vite build",
    "start": "npm run build && node server/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.0.0"
  }
}
